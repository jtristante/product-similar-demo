# Java 17 Profile Configuration
# Uses CompletableFuture with dedicated thread pool executor

# Thread pool configuration for Java 17 implementation
executor.core-pool-size=10
executor.max-pool-size=50
executor.queue-capacity=100
executor.keep-alive-seconds=60
